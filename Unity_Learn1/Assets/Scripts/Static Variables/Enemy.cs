using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/* Static members (such as variables and methods) are members that 
 * are shared amongst all instances of a class.
 * 
 * Furthermore, static members can be accessed directly from a class
 * without instantiating an object of the class first. 
 *  
 * Normally, member variables are unique to each object of a class, 
 * while every object of the class has the same variables, 
 * they each have their own values.
 * 
 * However, with static variables, 
 * 
 */


public class Enemy : MonoBehaviour
{
    
}
